<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<a href="/tasks">Go back to list</a>
<br>
<a href="/tasks/<%=@task.id%>/edit">Edit this task</a>
<br>
<%= link_to "🗑 Delete this task",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
